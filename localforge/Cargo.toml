[workspace]
resolver = "2"
members = ["daemon", "cli"]

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["LocalForge Contributors"]
license = "MIT"
repository = "https://github.com/localforge/localforge"
description = "Local-first developer substrate for identity, trust, config, and logging"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.43", features = ["full"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

# Cryptography
ed25519-dalek = { version = "2.1", features = ["rand_core"] }
rand = "0.8"

# Error handling
thiserror = "2.0"
anyhow = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json"] }

# CLI
clap = { version = "4.5", features = ["derive"] }

# Time
chrono = { version = "0.4", features = ["serde"] }

# UUID
uuid = { version = "1.11", features = ["v4", "serde"] }

# Platform paths
directories = "5.0"

# Base64
base64 = "0.22"
